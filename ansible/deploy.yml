- hosts: localhost
  become: true

  tasks:
    - name: Stop running container if exists
      shell: |
        docker stop java-app || true
        docker rm java-app || true

    - name: Run new container
      shell: docker run -d --restart always -p 8080:8080 --name java-app java-image
